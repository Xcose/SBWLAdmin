'use strict';

/**
 * lte-service-provider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::lte-service-provider.lte-service-provider');
