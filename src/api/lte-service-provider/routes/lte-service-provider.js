'use strict';

/**
 * lte-service-provider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::lte-service-provider.lte-service-provider');
